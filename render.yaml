services:
  - type: web
    name: electricity-dashboard
    env: python
    buildCommand: "pip install -r requirements.txt && python generate_sample_data.py && python data_prep.py"
    startCommand: "gunicorn app:app"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
